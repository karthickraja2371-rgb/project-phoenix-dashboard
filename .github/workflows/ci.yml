name: Project Phoenix CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-test-build:
    name: Build, Lint & Unit Test Verification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Code Linter (oxlint)
        run: npm run lint

      - name: Run Unit Tests (Vitest)
        run: npm run test

      - name: Run Production WebGL Build Verification (Vite)
        run: npm run build
